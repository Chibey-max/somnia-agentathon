{
  "name": "eth-agent-template",
  "private": true,
  "workspaces": [
    "runtime",
    "dashboard"
  ],
  "scripts": {
    "build": "npm run build -w runtime",
    "setup": "npm run setup -w runtime",
    "agent": "npm run agent -w runtime",
    "mcp": "npm run mcp -w runtime",
    "mcp:web": "npm run mcp:web -w runtime",
    "dashboard": "npm run dev -w dashboard",
    "dev": "npm run dev -w dashboard"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
